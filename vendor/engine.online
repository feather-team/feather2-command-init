<?php
//模版引擎配置
//此文件中出现所有路径，都相对于项目目录
return array(
	'templateDir' => __DIR__,	//模版目录，当前目录，一般不需要修改
	'suffix' => '#suffix#',	//模版后缀
	'tempDir' => '',	//模版引擎临时文件存放目录，可存放一切插件所产生的临时文件，设置此目录，模版引擎出错信息也可以更加清晰，方便调试
	'systemPlugins' => array(),
	//线上域名，此处只可设置一个域名，如需要设置多个域名，则需要通过设置文件domain属性来指定，并需要调用$view->set('自己定义的域名的key', '域名的值得');
	'staticDomain' => ''	
);