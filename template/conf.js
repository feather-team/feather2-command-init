feather.config.merge({
    project: {
        name: '${project.name}',
        charset: '${project.charset}'
    },

    template: {
        suffix: '${template.suffix}'
    },

    statics: '${statics}'
});